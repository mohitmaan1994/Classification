# Classification

I built a classifier that guesses whether a movie is romance or action, using only the numbers of times words appear in the movies's screenplay. The following algorithm and findings are used in the project:

1. Build a k-nearest-neighbors classifier.
2. Test a classifier on data.
